<UserControl x:Class="LearningUserControl.UserControls.CustomComboBoxTitleControl2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LearningUserControl.UserControls"
                 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Border CornerRadius="10" BorderThickness="2" BorderBrush="Green" Height="50" Width="150">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Label Content="Tiêu đề" FontSize="16" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="5 -15 0 0" Background="White"></Label>

            <TextBox Grid.Column="0" Padding="10" Text="Lựa chọn 1" FontSize="20"></TextBox>
            <Border BorderThickness="1 0 0 0"  BorderBrush="Green" Grid.Column="1">
                <materialDesign:PackIcon Kind="ArrowDown" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Border>
        </Grid>
    </Border>
</UserControl>